# GitHub OAuth連携設定（本番環境用）
# このファイルを .env.production にコピーして値を設定してください。
# GitHub Actionsでビルドする場合は、リポジトリのSecretsに設定してください。
# これらの環境変数が設定されていない場合、GitHub Gist連携機能は無効になります。
#
# 読み込みタイミング: npm run build（本番ビルド時）
# ※ GitHub Actionsでビルドする場合はSecretsから注入されるため、このファイルは不要です。

# GitHub OAuth AppのClient ID
# OAuth Appを作成してClient IDを設定してください
# https://github.com/settings/applications/new
# 本番用のCallback URLは https://your-domain.github.io/keyboard-layout-studio/callback を設定
VITE_GITHUB_CLIENT_ID=

# Cloudflare Worker URL for OAuth token exchange
# Cloudflare WorkersにデプロイしたWorkerのURLを設定してください
# 例: https://oauth-token-exchange.your-subdomain.workers.dev
VITE_OAUTH_WORKER_URL=
